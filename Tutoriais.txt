Usar por cmd

No powershell utilizar comando
iex (new-object net.webclient).downloadstring('https://get.scoop.sh')

Também no power shell executar
scoop install hub

No CMD md diretorio

vi README.md
git init

hub create -d "nome_do_repo"

git add README.md
git commit -m "first commit"
git remote add origin git@github.com:user/<reponame>.git
git push -u origin master


git clone "link" --baixa repositorio
git add "arquivo"
git rm "arquivo"
git status --checar
git commit -m "mensagem"
git push --upload
git pull --download
